import { expect } from 'chai';

describe('Integration tests', () => {

    it('test 1', () => {
        expect(1).to.equal(1);
    });
    it('test 2', () => {
        expect(1).to.equal(1);
    });
    it('test 3', () => {
        expect(1).to.equal(1);
    });

});